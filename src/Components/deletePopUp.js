import React from 'react';
import { Typography } from '@mui/material';
import './deletePopUp.css';

export default function GreySheer({ onDelete, onJoin, onRequests, onEdit }) {
  return (
    <div>

      <div className="sheer-grey-background">
        <h2>here</h2>
      </div>
    </div>
  );
}
